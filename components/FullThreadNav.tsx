import React from 'react'
import styles from '../styles/FullThreadNav.module.css'

const FullThreadNav = () => {
	return (
		<nav className={`${styles.nav__container}`}>
			<h1 className={`${styles.sender__name}`}>Messenger Name</h1>
		</nav>
	)
}

export default FullThreadNav
